import { logDOM } from "@testing-library/react";
import ReactLogo from './logo.svg'

const Cameron = () => {
    return(
        <div>
            <h2>Cameron</h2>
            <img src = {ReactLogo}/>
        </div>
    );
}

export default Cameron;